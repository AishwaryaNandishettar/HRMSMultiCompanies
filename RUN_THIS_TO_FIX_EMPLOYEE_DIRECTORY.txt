═══════════════════════════════════════════════════════════════
  FIX EMPLOYEE DIRECTORY - SHOWS WRONG EMPLOYEES
═══════════════════════════════════════════════════════════════

PROBLEM:
  MongoDB has: "Lata Benakop (IT-EMP-0041)"
  Frontend shows: "Rahul Sharma (EMP101)"
  
  They don't match!

═══════════════════════════════════════════════════════════════

QUICK FIX (Choose One):

OPTION 1: Run Batch File (Easiest)
──────────────────────────────────
1. Open Command Prompt (cmd)
2. Run this command:

   cd "d:\New folder\HRMSProject (2)\HRMSProject\HRMS-Backend"
   fix_employee_directory.bat

3. Press Enter when prompted
4. Wait for "FIX COMPLETED!" message


OPTION 2: Run MongoDB Script
──────────────────────────────────
1. Open Command Prompt (cmd)
2. Run this command:

   cd "d:\New folder\HRMSProject (2)\HRMSProject\HRMS-Backend"
   mongosh "mongodb://localhost:27017/Data_base_hrms" fix_employee_companyid.js

3. Wait for "SUCCESS!" message


OPTION 3: Manual MongoDB Commands
──────────────────────────────────
1. Open Command Prompt (cmd)
2. Run: mongosh "mongodb://localhost:27017/Data_base_hrms"
3. Run: use Data_base_hrms
4. Run: db.employees.updateMany({}, { $set: { companyId: "omoikaneinnovations" }})
5. Run: db.users.updateMany({ role: { $ne: "ADMIN" }}, { $set: { companyId: "omoikaneinnovations" }})

═══════════════════════════════════════════════════════════════

AFTER RUNNING FIX:

STEP 1: Restart Backend
──────────────────────────────────
1. Stop current backend (Ctrl+C)
2. Run:
   cd "d:\New folder\HRMSProject (2)\HRMSProject\HRMS-Backend"
   mvn spring-boot:run


STEP 2: Clear Browser Cache
──────────────────────────────────
1. Press F12 (Open DevTools)
2. Go to "Application" tab
3. Click "Clear site data"
4. Close and reopen browser


STEP 3: Test
──────────────────────────────────
1. Go to http://localhost:5173
2. Login
3. Go to Employee Directory
4. Should now show correct employees!

═══════════════════════════════════════════════════════════════

WHAT THIS DOES:
✅ Updates all employees to same companyId
✅ Updates all users to same companyId
✅ No code logic changed
✅ Safe to run

═══════════════════════════════════════════════════════════════
